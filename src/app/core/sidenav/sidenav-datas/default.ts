import { NavigationItem } from "../sidenav.component";
import { SidenavData } from "./sidenav-data.model";

const navigationData: NavigationItem[] = [];

export const defaultData: SidenavData = {
  icon: "",
  title: "",
  shouldHave: false,
  navigationData
};
